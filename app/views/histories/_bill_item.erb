<% @bills.each do |item| %>
  <tr>
    <td><%= formatted_created_at(item.created_at) %></td>
    <td class="text-center"><%= number_to_currency(item.amount) %></td>
    <td><%= status_label(item.status) %></td>
    <td><%= order_action_buttons(item) %></td>
  </tr>
<% end %>
